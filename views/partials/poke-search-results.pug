.row.d-flex.p-1.p-md-2.m-md-3.text-center
    - let i;
    - for (i=0; i<results.length; i++)
        .col-sm-4.py-3
            a(href='/pokemon/'+results[i].pokemon_id).text-capitalize #{results[i].pokemon_id} - #{results[i].pokemon_name}
                img(src=`/images/poke-icons/${results[i].pokemon_id}.png`, alt="")
